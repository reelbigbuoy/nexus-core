# Data Inspector Plugin

A lightweight live view into the Nexus shared `DataStore`.

## Current features
- Lists all keys currently in the shared data store
- Shows the selected key value as formatted JSON
- Live updates as plugins write shared state
- Search/filter for keys
- Persists selected key and splitter sizes