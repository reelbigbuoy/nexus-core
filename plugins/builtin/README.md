# Built-in Plugins

This directory stores platform-shipped utility plugins for Nexus Core.

Built-in plugins should remain generic and domain-neutral. They may support platform inspection, property inspection, diagnostics, or other reusable workspace functions.

Each plugin should live in its own subdirectory and include a `plugin.json` manifest.
