# Nexus Plugins

Nexus loads plugins from source-specific subfolders under this directory.

## Layout
- `builtin/` — host-shipped platform panels and built-in tools

Each plugin resides in its own folder with a `plugin.json` manifest.